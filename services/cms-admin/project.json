{
  "name": "cms-admin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "services/cms-admin/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/services/cms-admin"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nrwl/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cms-admin:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cms-admin:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "cms-admin:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nrwl/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "cms-admin:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "cms-admin:build:development"
        },
        "production": {
          "buildTarget": "cms-admin:build:production"
        }
      }
    },
    "test": {
      "executor": "@nrwl/vite:test",
      "outputs": ["coverage/services/cms-admin"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/services/cms-admin"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["services/cms-admin/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
